:concurrency: 3
:timeout: 60
:verbose: true
:queues:
  - default
:scheduler:
  :schedule:
    DailyGenerationJob:
      enabled: false
      every: 60s
      queue: default
      description: "Generate daily Open AI image jeopardy"
